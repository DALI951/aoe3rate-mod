# runtime logs — never commit
*.log
*d3d9mod.log
Temp/

# build artifacts (compiled probes/tests) — never commit
*.exe
*.o
*.obj
*.d

# the Ship of Theseus carries the original d3dx9_25.dll — that one is NOT ours, don't commit it
d3dx9_25.dll

# transient build/test notes
build_log.txt
build_test.txt
tests/build_actual.txt

# generated files — never commit
ResourceRateMod.ini
d3d9.sha256
verify_log.txt

# duplicate copy of the proxy used as a local test fixture — the shipped binary lives in the repo root
tests/d3d9.dll